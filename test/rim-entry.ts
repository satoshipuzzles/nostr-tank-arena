// Bundle entry for test/rim.mjs: the arena and the sim together, because the
// edgeless rules live in both — the flag and the rails in arena.ts, the
// missing clamp and the shell margin in sim.ts.
export * from '../src/arena'
export * from '../src/sim'
